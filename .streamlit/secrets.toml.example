# Copy this file to .streamlit/secrets.toml (never commit the real one).
# approval_gate.py checks GROQ_API_KEY first per spec, then GEMINI_API_KEY
# (which is what draft_machine.py drafts with today). Environment variables
# and the sidebar password field also work.
GROQ_API_KEY = ""
GEMINI_API_KEY = ""
